const defaultState = {
  appName: "Medium-rare"
};

const commonReducer = (state = defaultState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default commonReducer;
